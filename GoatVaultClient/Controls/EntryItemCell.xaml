<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="GoatVaultClient.Controls.EntryItemCell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:m="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    x:Name="This">
    <Border
        BackgroundColor="Transparent"
        Stroke="Transparent"
        StrokeThickness="0">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="{StaticResource xSmall}" />
        </Border.StrokeShape>
        <Grid
            Padding="{x:StaticResource pContent}"
            ColumnDefinitions="40, *, Auto"
            ColumnSpacing="{StaticResource sContent}"
            VerticalOptions="Center">

            <Image
                Grid.Column="0"
                HeightRequest="{x:StaticResource iconDesktop}"
                HorizontalOptions="Start"
                VerticalOptions="Center"
                WidthRequest="{x:StaticResource iconDesktop}">
                <Image.Source>
                    <FontImageSource
                        FontFamily="MaterialRounded"
                        Glyph="{Binding IconGlyph, Source={x:Reference This}}"
                        Size="{x:StaticResource iconDesktopResolution}"
                        Color="{AppThemeBinding Light=Black,
                                                Dark=White}" />
                </Image.Source>
            </Image>

            <Label
                Grid.Column="1"
                LineBreakMode="TailTruncation"
                Style="{x:StaticResource MaterialTitleMedium}"
                Text="{Binding Text, Source={x:Reference This}}"
                VerticalOptions="Center" />

            <Border
                Grid.Column="2"
                Padding="{StaticResource sContent}"
                BackgroundColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLow},
                                                  Dark={StaticResource DarkSurfaceContainerLow}}"
                Stroke="{AppThemeBinding Light={StaticResource LightSurfaceContainerLow},
                                         Dark={StaticResource DarkSurfaceContainerLow}}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="{StaticResource xSmall}" />
                </Border.StrokeShape>
                <HorizontalStackLayout HorizontalOptions="End" Spacing="{StaticResource sContent}">
                    <Image
                        Grid.Column="0"
                        HeightRequest="{x:StaticResource iconDesktop}"
                        HorizontalOptions="Start"
                        VerticalOptions="Center"
                        WidthRequest="{x:StaticResource iconDesktop}">
                        <Image.Source>
                            <FontImageSource
                                FontFamily="MaterialRounded"
                                Glyph="{x:Static m:MaterialRounded.Folder}"
                                Size="{x:StaticResource iconDesktopResolution}"
                                Color="{AppThemeBinding Light={StaticResource LightOnSecondaryContainer},
                                                        Dark={StaticResource DarkOnSecondaryContainer}}" />
                        </Image.Source>
                    </Image>
                    <Label
                        LineBreakMode="TailTruncation"
                        Style="{x:StaticResource MaterialLabelMedium}"
                        Text="{Binding CategoryName, Source={x:Reference This}}"
                        VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Border>
        </Grid>
    </Border>
</ContentView>

<?xml version="1.0" encoding="utf-8" ?>
<uranium:UraniumContentPage
    x:Class="GoatVaultClient.Pages.SecurityPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:GoatVaultClient.Controls"
    xmlns:local="clr-namespace:GoatVaultClient.Pages"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
    xmlns:m="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:vm="clr-namespace:GoatVaultClient.ViewModels"
    x:DataType="vm:SecurityPageViewModel"
    BackgroundColor="{AppThemeBinding Light={StaticResource LightSurface},
                                      Dark={StaticResource DarkSurface}}">
    
    

    <ScrollView>
        <VerticalStackLayout Padding="{StaticResource pLarge}" Spacing="{StaticResource sLarge}">
            <controls:Component
                Title="Vault Score"
                BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLowest},
                                          Dark={StaticResource DarkSurfaceContainerLowest}}"
                ContentPadding="{StaticResource sContent}"
                HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLow},
                                                Dark={StaticResource DarkSurfaceContainerLow}}"
                ShowHeader="True">

                <!--  Vault Score Section  -->
                <VerticalStackLayout HorizontalOptions="Fill" Spacing="10">
                    <Label
                        Margin="{StaticResource mLarge}"
                        HorizontalOptions="Center"
                        Style="{StaticResource MaterialDisplayMedium}"
                        Text="{Binding VaultTierText}" />
                    <FlexLayout
                        AlignContent="Start"
                        AlignItems="Start"
                        Direction="Row"
                        JustifyContent="SpaceEvenly"
                        Wrap="Wrap">

                        <!--  Master Password Strength  -->
                        <controls:Component
                            Title="Master Password Strength"
                            Margin="{StaticResource sContent}"
                            BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerHigh},
                                                      Dark={StaticResource DarkSurfaceContainerHigh}}"
                            ContentPadding="{StaticResource sLarge}"
                            FlexLayout.Basis="300"
                            HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainer},
                                                            Dark={StaticResource DarkSurfaceContainer}}"
                            ShowHeader="True">
                            <VerticalStackLayout>
                                <VerticalStackLayout>
                                    <Label
                                        HorizontalOptions="Center"
                                        Style="{StaticResource MaterialDisplaySmall}"
                                        Text="{Binding MasterPasswordStrength}"
                                        TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                    Dark={StaticResource DarkOnSurface}}" />
                                    <Slider
                                        HorizontalOptions="Fill"
                                        InputTransparent="True"
                                        Maximum="400"
                                        Minimum="0"
                                        Value="{Binding MasterPasswordStrength}" />
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label
                                            HorizontalOptions="Start"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="0"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                        <Label
                                            Grid.Column="1"
                                            HorizontalOptions="Center"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="400"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                    </Grid>
                                </VerticalStackLayout>
                                <Label
                                    HorizontalOptions="Center"
                                    Style="{StaticResource MaterialLabelSmall}"
                                    Text="{Binding MasterPasswordCategory}"
                                    TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                Dark={StaticResource DarkOnSurfaceVariant}}" />
                            </VerticalStackLayout>
                        </controls:Component>

                        <!--  Average Password Strength  -->
                        <controls:Component
                            Title="Average Password Strength"
                            Margin="{StaticResource sContent}"
                            BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerHigh},
                                                      Dark={StaticResource DarkSurfaceContainerHigh}}"
                            ContentPadding="{StaticResource sLarge}"
                            FlexLayout.Basis="300"
                            HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainer},
                                                            Dark={StaticResource DarkSurfaceContainer}}"
                            ShowHeader="True">
                            <VerticalStackLayout>
                                <VerticalStackLayout>
                                    <Label
                                        HorizontalOptions="Center"
                                        Style="{StaticResource MaterialDisplaySmall}"
                                        Text="{Binding AveragePasswordsStrength}"
                                        TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                    Dark={StaticResource DarkOnSurface}}" />
                                    <Slider
                                        HorizontalOptions="Fill"
                                        InputTransparent="True"
                                        Maximum="200"
                                        Minimum="0"
                                        Value="{Binding AveragePasswordsStrength}" />
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label
                                            HorizontalOptions="Start"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="0"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                        <Label
                                            Grid.Column="1"
                                            HorizontalOptions="Center"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="200"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                    </Grid>
                                </VerticalStackLayout>
                                <Label
                                    HorizontalOptions="Center"
                                    Style="{StaticResource MaterialLabelSmall}"
                                    Text="{Binding AveragePasswordsCategory}"
                                    TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                Dark={StaticResource DarkOnSurfaceVariant}}" />
                            </VerticalStackLayout>
                        </controls:Component>

                        <!--  Originality Strength  -->
                        <controls:Component
                            Title="Password Originality"
                            Margin="{StaticResource sContent}"
                            BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerHigh},
                                                      Dark={StaticResource DarkSurfaceContainerHigh}}"
                            ContentPadding="{StaticResource sLarge}"
                            FlexLayout.Basis="300"
                            HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainer},
                                                            Dark={StaticResource DarkSurfaceContainer}}"
                            ShowHeader="True">
                            <VerticalStackLayout>
                                <VerticalStackLayout>
                                    <Label
                                        HorizontalOptions="Center"
                                        Style="{StaticResource MaterialDisplaySmall}"
                                        Text="{Binding ReuseRate}"
                                        TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                    Dark={StaticResource DarkOnSurface}}" />
                                    <Slider
                                        HorizontalOptions="Fill"
                                        InputTransparent="True"
                                        Maximum="200"
                                        Minimum="0"
                                        Value="{Binding ReuseRate}" />
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label
                                            HorizontalOptions="Start"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="0"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                        <Label
                                            Grid.Column="1"
                                            HorizontalOptions="Center"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="200"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                    </Grid>
                                </VerticalStackLayout>
                                <Label
                                    HorizontalOptions="Center"
                                    Style="{StaticResource MaterialLabelSmall}"
                                    Text="{Binding OriginalityCategory}"
                                    TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                Dark={StaticResource DarkOnSurfaceVariant}}" />
                            </VerticalStackLayout>
                        </controls:Component>

                        <!--  Compromised Passwords  -->
                        <controls:Component
                            Title="Compromised Passwords"
                            Margin="{StaticResource sContent}"
                            BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerHigh},
                                                      Dark={StaticResource DarkSurfaceContainerHigh}}"
                            ContentPadding="{StaticResource sLarge}"
                            FlexLayout.Basis="300"
                            HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainer},
                                                            Dark={StaticResource DarkSurfaceContainer}}"
                            ShowHeader="True">
                            <VerticalStackLayout>
                                <VerticalStackLayout>
                                    <Label
                                        HorizontalOptions="Center"
                                        Style="{StaticResource MaterialDisplaySmall}"
                                        Text="{Binding BreachesCount}"
                                        TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                    Dark={StaticResource DarkOnSurface}}" />
                                    <Slider
                                        HorizontalOptions="Fill"
                                        InputTransparent="True"
                                        Maximum="100"
                                        Minimum="0"
                                        Value="{Binding BreachesCount}" />
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label
                                            HorizontalOptions="Start"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="0"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                        <Label
                                            Grid.Column="1"
                                            HorizontalOptions="Center"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="100"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                    </Grid>
                                </VerticalStackLayout>
                                <Label
                                    HorizontalOptions="Center"
                                    Style="{StaticResource MaterialLabelSmall}"
                                    Text="{Binding BreachesCategory}"
                                    TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                Dark={StaticResource DarkOnSurfaceVariant}}" />
                            </VerticalStackLayout>
                        </controls:Component>

                        <!--  MFA  -->
                        <controls:Component
                            Title="Multi-Factor Authentication"
                            Margin="{StaticResource sContent}"
                            BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerHigh},
                                                      Dark={StaticResource DarkSurfaceContainerHigh}}"
                            ContentPadding="{StaticResource sLarge}"
                            FlexLayout.Basis="300"
                            HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainer},
                                                            Dark={StaticResource DarkSurfaceContainer}}"
                            ShowHeader="True">
                            <VerticalStackLayout>
                                <VerticalStackLayout>
                                    <Label
                                        HorizontalOptions="Center"
                                        Style="{StaticResource MaterialDisplaySmall}"
                                        Text="{Binding MfaPercent}"
                                        TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                    Dark={StaticResource DarkOnSurface}}" />
                                    <Slider
                                        HorizontalOptions="Fill"
                                        InputTransparent="True"
                                        Maximum="200"
                                        Minimum="0"
                                        Value="{Binding MfaPercent}" />
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label
                                            HorizontalOptions="Start"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="0"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                        <Label
                                            Grid.Column="1"
                                            HorizontalOptions="Center"
                                            Style="{StaticResource MaterialLabelSmall}"
                                            Text="200"
                                            TextColor="{AppThemeBinding Light={StaticResource LightSurfaceDim},
                                                                        Dark={StaticResource DarkSurfaceDim}}" />
                                    </Grid>
                                </VerticalStackLayout>
                                <Label
                                    HorizontalOptions="Center"
                                    Style="{StaticResource MaterialLabelSmall}"
                                    Text="{Binding MfaCategory}"
                                    TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                Dark={StaticResource DarkOnSurfaceVariant}}" />
                            </VerticalStackLayout>
                        </controls:Component>
                    </FlexLayout>
                </VerticalStackLayout>
            </controls:Component>

            <!--  Account Credentials Section  -->
            <controls:Component
                Title="Account Credentials"
                BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLowest},
                                          Dark={StaticResource DarkSurfaceContainerLowest}}"
                ContentPadding="{StaticResource sContent}"
                HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLow},
                                                Dark={StaticResource DarkSurfaceContainerLow}}"
                ShowHeader="True">
                <VerticalStackLayout Padding="20" Spacing="20">

                    <!--  Email  -->
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="{StaticResource sLarge}">
                        <Label
                            FontAttributes="Bold"
                            Text="Email"
                            VerticalOptions="Center" />
                        <Label
                            Grid.Column="1"
                            FontSize="14"
                            HorizontalOptions="End"
                            LineBreakMode="TailTruncation"
                            Text="{Binding Email}"
                            VerticalOptions="Center" />
                        <controls:ImageButton
                            Grid.Column="2"
                            ButtonBgColor="{AppThemeBinding Light={StaticResource LightPrimaryContainer},
                                                            Dark={StaticResource DarkPrimaryContainer}}"
                            Command="{Binding EditEmailCommand}"
                            IconColor="{AppThemeBinding Light={StaticResource LightOnPrimaryContainer},
                                                        Dark={StaticResource DarkOnPrimaryContainer}}"
                            IconGlyph="{x:Static m:MaterialRounded.Edit}" />
                    </Grid>

                    <!--  Master Password  -->
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="{StaticResource sLarge}">
                        <Label
                            FontAttributes="Bold"
                            Text="Master Password"
                            VerticalOptions="Center" />
                        <Label
                            Grid.Column="1"
                            FontSize="14"
                            HorizontalOptions="End"
                            Text="••••••••••••"
                            VerticalOptions="Center" />
                        <controls:ImageButton
                            Grid.Column="2"
                            ButtonBgColor="{AppThemeBinding Light={StaticResource LightPrimaryContainer},
                                                            Dark={StaticResource DarkPrimaryContainer}}"
                            Command="{Binding EditMasterPasswordCommand}"
                            IconColor="{AppThemeBinding Light={StaticResource LightOnPrimaryContainer},
                                                        Dark={StaticResource DarkOnPrimaryContainer}}"
                            IconGlyph="{x:Static m:MaterialRounded.Edit}" />
                    </Grid>
                </VerticalStackLayout>
            </controls:Component>

            <!--  Two-Factor Authentication Section  -->
            <controls:Component
                Title="MFA"
                BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLowest},
                                          Dark={StaticResource DarkSurfaceContainerLowest}}"
                ContentPadding="{StaticResource sContent}"
                HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLow},
                                                Dark={StaticResource DarkSurfaceContainerLow}}"
                ShowHeader="True">
                <VerticalStackLayout Padding="20" Spacing="15">
                    <!--  MFA Status  -->
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                        <Label
                            FontAttributes="Bold"
                            FontSize="14"
                            Text="Status"
                            VerticalOptions="Center" />
                        <HorizontalStackLayout Grid.Column="1" Spacing="8">
                            <Border
                                BackgroundColor="{Binding MfaEnabled, Converter={StaticResource BoolToColorConverter}}"
                                HeightRequest="12"
                                StrokeShape="RoundRectangle 6"
                                VerticalOptions="Center"
                                WidthRequest="12" />
                            <Label
                                FontSize="14"
                                Text="{Binding MfaEnabled, Converter={StaticResource BoolToMfaStatusConverter}}"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>
                    </Grid>

                    <!--  MFA Description  -->
                    <Label
                        FontSize="12"
                        LineBreakMode="WordWrap"
                        Text="Two-factor authentication adds an extra layer of security to your account by requiring a time-based code from your authenticator app in addition to your password."
                        TextColor="{AppThemeBinding Light={StaticResource Gray500},
                                                    Dark={StaticResource Gray400}}" />

                    <!--  Enable/Disable Button  -->
                    <Button
                        Margin="0,10,0,0"
                        BackgroundColor="{AppThemeBinding Light={StaticResource LightSecondaryContainer},
                                                          Dark={StaticResource DarkSecondaryContainer}}"
                        Command="{Binding EnableMfaCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        IsVisible="{Binding MfaEnabled, Converter={StaticResource InvertedBoolConverter}}"
                        Text="{Binding MfaEnabled, Converter={StaticResource BoolToMfaButtonTextConverter}}"
                        TextColor="{AppThemeBinding Light={StaticResource LightOnSecondaryContainer},
                                                    Dark={StaticResource DarkOnSecondaryContainer}}" />

                    <Button
                        Margin="0,10,0,0"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Error},
                                                          Dark={StaticResource ErrorDark}}"
                        Command="{Binding DisableMfaCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        IsVisible="{Binding MfaEnabled}"
                        Text="Disable MFA"
                        TextColor="{AppThemeBinding Light={StaticResource LightOnSecondaryContainer},
                                                    Dark={StaticResource DarkOnSecondaryContainer}}" />

                </VerticalStackLayout>
            </controls:Component>

            <controls:Component
                Title="Shamir Backup"
                BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLowest},
                                          Dark={StaticResource DarkSurfaceContainerLowest}}"
                ContentPadding="{StaticResource sContent}"
                HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLow},
                                                Dark={StaticResource DarkSurfaceContainerLow}}"
                ShowHeader="True">
                <VerticalStackLayout Padding="20" Spacing="15">
                    <!--  Shamir Status  -->
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                        <Label
                            FontAttributes="Bold"
                            FontSize="14"
                            Text="Status"
                            VerticalOptions="Center" />
                        <HorizontalStackLayout Grid.Column="1" Spacing="8">
                            <Border
                                BackgroundColor="{Binding ShamirEnabled, Converter={StaticResource BoolToColorConverter}}"
                                HeightRequest="12"
                                StrokeShape="RoundRectangle 6"
                                VerticalOptions="Center"
                                WidthRequest="12" />
                            <Label
                                FontSize="14"
                                Text="{Binding ShamirEnabled, Converter={StaticResource BoolToMfaStatusConverter}}"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>
                    </Grid>

                    <!--  Shamir Description  -->
                    <Label
                        FontSize="12"
                        LineBreakMode="WordWrap"
                        Text="Shamir Backup splits your master password into several unique 'shares', requiring a specific minimum number of them to be reunited to reconstruct the original key."
                        TextColor="{AppThemeBinding Light={StaticResource Gray500},
                                                    Dark={StaticResource Gray400}}" />

                    <!--  Enable/Disable Button  -->
                    <Button
                        Margin="0,10,0,0"
                        BackgroundColor="{AppThemeBinding Light={StaticResource LightPrimary},
                                                          Dark={StaticResource DarkPrimary}}"
                        Command="{Binding EnableShamirCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        IsVisible="{Binding ShamirEnabled, Converter={StaticResource InvertedBoolConverter}}"
                        Text="{Binding ShamirEnabled, Converter={StaticResource BoolToShamirButtonTextConverter}}"
                        TextColor="White" />

                    <Button
                        Margin="0,10,0,0"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Error},
                                                          Dark={StaticResource ErrorDark}}"
                        Command="{Binding DisableShamirCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        IsVisible="{Binding MfaEnabled}"
                        Text="Enable Shamir Backup"
                        TextColor="White" />

                </VerticalStackLayout>
            </controls:Component>

            <!--  Danger Zone Section  -->
            <controls:Component
                Title="Danger Zone"
                BgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLowest},
                                          Dark={StaticResource DarkSurfaceContainerLowest}}"
                ContentPadding="{StaticResource sContent}"
                HeaderBgColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLow},
                                                Dark={StaticResource DarkSurfaceContainerLow}}"
                ShowHeader="True">
                <VerticalStackLayout Padding="20" Spacing="15">

                    <!--  Wipe Vault  -->
                    <Label
                        FontAttributes="Bold"
                        FontSize="14"
                        Text="Wipe Vault"
                        VerticalOptions="Center" />

                    <Label
                        FontSize="12"
                        LineBreakMode="WordWrap"
                        Text="Wipe all passwords and categories in your vault. This action will apply on your device and on the cloud, and cannot be reversed!"
                        TextColor="{AppThemeBinding Light={StaticResource Gray500},
                                                    Dark={StaticResource Gray400}}" />

                    <Button
                        Margin="0,10,0,10"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Error},
                                                          Dark={StaticResource ErrorDark}}"
                        Command="{Binding WipeVaultCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        Text="Wipe Vault"
                        TextColor="White" />

                    <!--  Delete Account  -->
                    <Label
                        FontAttributes="Bold"
                        FontSize="14"
                        Text="Delete Account"
                        VerticalOptions="Center" />

                    <Label
                        FontSize="12"
                        LineBreakMode="WordWrap"
                        Text="Permanently delete your GoatVault account. This action will apply on your device and on the cloud, and cannot be reversed!"
                        TextColor="{AppThemeBinding Light={StaticResource Gray500},
                                                    Dark={StaticResource Gray400}}" />

                    <Button
                        Margin="0,10,0,0"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Error},
                                                          Dark={StaticResource ErrorDark}}"
                        Command="{Binding DeleteAccountCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        Text="Delete Account"
                        TextColor="White" />
                </VerticalStackLayout>
            </controls:Component>
        </VerticalStackLayout>
    </ScrollView>
</uranium:UraniumContentPage>

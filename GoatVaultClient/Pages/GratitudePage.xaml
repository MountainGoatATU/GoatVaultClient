<uranium:UraniumContentPage
    x:Class="GoatVaultClient.Pages.GratitudePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:vm="clr-namespace:GoatVaultClient.ViewModels"
    Title="GratitudePage"
    x:DataType="vm:GratitudePageViewModel">

    <Grid Padding="30">
        <VerticalStackLayout
            HorizontalOptions="Center"
            Spacing="30"
            VerticalOptions="Center">

            <Label
                FontAttributes="Bold"
                FontSize="28"
                HorizontalOptions="Center"
                Text="Check your email!" />
            <Label
                FontSize="16"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
                Text="We have sent a verification email to your inbox. Please verify your email and then log in." />

            <Button
                Margin="0,10,0,0"
                BackgroundColor="{AppThemeBinding Light={StaticResource LightSecondaryContainer},
                                                  Dark={StaticResource DarkSecondaryContainer}}"
                Command="{Binding ContinueCommand}"
                Text="Start using GoatVault"
                TextColor="{AppThemeBinding Light={StaticResource LightOnSecondaryContainer},
                                            Dark={StaticResource DarkOnSecondaryContainer}}" />

        </VerticalStackLayout>
    </Grid>
</uranium:UraniumContentPage>
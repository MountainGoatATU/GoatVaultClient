<?xml version="1.0" encoding="utf-8" ?>
<uranium:UraniumContentPage
    x:Class="GoatVaultClient.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:GoatVaultClient.Controls"
    xmlns:input="clr-namespace:InputKit.Shared.Controls;assembly=InputKit.Maui"
    xmlns:m="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:models="clr-namespace:GoatVaultCore.Models;assembly=GoatVaultCore"
    xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
    xmlns:validation="clr-namespace:InputKit.Shared.Validations;assembly=InputKit.Maui"
    xmlns:vm="clr-namespace:GoatVaultClient.ViewModels"
    x:DataType="vm:LoginPageViewModel"
    BackgroundColor="{AppThemeBinding Light={StaticResource LightSurface},
                                      Dark={StaticResource DarkSurface}}">

    <ScrollView HorizontalOptions="Center" VerticalOptions="Center">

        <Grid Padding="{OnIdiom Phone={StaticResource pSmall}, Desktop={StaticResource pLarge}}">
            <VerticalStackLayout Spacing="{StaticResource formSpacing}">
            <Border
                Padding="{StaticResource sLarge}"
                BackgroundColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLowest},
                                                  Dark={StaticResource DarkSurfaceContainerLowest}}"
                HorizontalOptions="Center"
                Stroke="{AppThemeBinding Light={StaticResource LightOutlineVariant},
                                         Dark={StaticResource DarkOutlineVariant}}"
                StrokeThickness="1"
                VerticalOptions="Center"
                WidthRequest="{OnIdiom Phone=360,
                                       Desktop=450}">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="{StaticResource Large}" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="{StaticResource formSpacing}">
                    <input:FormView SubmitCommand="{Binding LoginCommand}">
                        <VerticalStackLayout IsVisible="{Binding IsOnline}" Spacing="{StaticResource formSpacing}">
                            <Label
                                Margin="0,0,0,8"
                                HorizontalOptions="Center"
                                Style="{StaticResource MaterialDisplaySmall}"
                                Text="Sign In" />

                            <material:TextField
                                Title="Email"
                                CornerRadius="{StaticResource xSmall}"
                                Text="{Binding EmailText}">
                                <validation:RequiredValidation />
                                <validation:RegexValidation Message="Please type a valid e-mail address." Pattern="{x:Static input:AdvancedEntry.REGEX_EMAIL}" />
                            </material:TextField>

                            <material:TextField
                                Title="Password"
                                CornerRadius="{StaticResource xSmall}"
                                IsPassword="True"
                                Text="{Binding Password}">
                                <validation:RequiredValidation />
                                <material:TextField.Attachments>
                                    <material:TextFieldPasswordShowHideAttachment />
                                </material:TextField.Attachments>
                            </material:TextField>

                            <Button
                                input:FormView.IsSubmitButton="True"
                                StyleClass="FilledButton"
                                Text="Login" />

                        </VerticalStackLayout>
                    </input:FormView>

                    <input:FormView SubmitCommand="{Binding LoginCommand}">
                        <VerticalStackLayout IsVisible="{Binding IsOnline, Converter={StaticResource InvertedBoolConverter}}" Spacing="{StaticResource formSpacing}">
                            <Label Style="{StaticResource MaterialHeadlineSmall}" Text="Offline Login" />

                            <Border
                                Padding="16"
                                BackgroundColor="{AppThemeBinding Light={StaticResource LightSurfaceVariant},
                                                                  Dark={StaticResource DarkSurfaceVariant}}"
                                StrokeThickness="0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="{StaticResource Medium}" />
                                </Border.StrokeShape>

                                <VerticalStackLayout Spacing="4">
                                    <Label
                                        FontSize="12"
                                        Text="Selected User:"
                                        TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                                    Dark={StaticResource DarkOnSurfaceVariant}}" />
                                    <Label
                                        FontAttributes="Bold"
                                        FontSize="16"
                                        Text="{Binding SelectedUser.Email}"
                                        TextColor="{AppThemeBinding Light={StaticResource LightOnSurface},
                                                                    Dark={StaticResource DarkOnSurface}}" />
                                </VerticalStackLayout>
                            </Border>

                            <material:TextField
                                Title="Password"
                                CornerRadius="{StaticResource xSmall}"
                                IsPassword="True"
                                Text="{Binding OfflinePassword}">
                                <material:TextField.Attachments>
                                    <material:TextFieldPasswordShowHideAttachment />
                                </material:TextField.Attachments>
                            </material:TextField>

                            <Button
                                input:FormView.IsSubmitButton="True"
                                StyleClass="FilledButton"
                                Text="Login Offline" />

                            <Button
                                Command="{Binding ClearSelectionCommand}"
                                IsVisible="{Binding IsOnline}"
                                StyleClass="FilledButton"
                                Text="Cancel" />
                        </VerticalStackLayout>
                    </input:FormView>

                    <VerticalStackLayout Spacing="{StaticResource sContent}">
                        <HorizontalStackLayout
                            Margin="0,4,0,0"
                            HorizontalOptions="Center"
                            IsVisible="{Binding IsOnline}"
                            Spacing="4">
                            <Label
                                Text="Don't have an account yet?"
                                TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                            Dark={StaticResource DarkOnSurfaceVariant}}"
                                VerticalOptions="Center" />
                            <Button
                                Padding="8,0"
                                BackgroundColor="Transparent"
                                BorderColor="Transparent"
                                Command="{Binding GoToRegisterCommand}"
                                FontFamily="{StaticResource MaterialMediumFont}"
                                FontSize="{StaticResource tMedium}"
                                StyleClass="TextButton"
                                Text="Register"
                                TextColor="{AppThemeBinding Light={StaticResource LightPrimary},
                                                            Dark={StaticResource DarkPrimary}}" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout
                            HorizontalOptions="Center"
                            IsVisible="{Binding IsOnline}"
                            Spacing="{StaticResource sContent}">
                            <Label
                                Text="Forgot password?"
                                TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                            Dark={StaticResource DarkOnSurfaceVariant}}"
                                VerticalOptions="Center" />
                            <Button
                                Padding="8,0"
                                BackgroundColor="Transparent"
                                BorderColor="Transparent"
                                Command="{Binding GoToRecoverCommand}"
                                FontFamily="{StaticResource MaterialMediumFont}"
                                FontSize="{StaticResource tMedium}"
                                Text="Recover"
                                TextColor="{AppThemeBinding Light={StaticResource LightPrimary},
                                                            Dark={StaticResource DarkPrimary}}" />
                        </HorizontalStackLayout>

                        <VerticalStackLayout
                            HorizontalOptions="Center"
                            IsVisible="{Binding IsBusy}"
                            VerticalOptions="Center">
                            <ActivityIndicator IsRunning="{Binding IsBusy}" />
                            <Label
                                Padding="0,20,0,0"
                                Text="Logging in..."
                                TextColor="{AppThemeBinding Light={StaticResource LightPrimary},
                                                            Dark={StaticResource DarkPrimary}}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Border
                Padding="{StaticResource sLarge}"
                BackgroundColor="{AppThemeBinding Light={StaticResource LightSurfaceContainerLowest},
                                                  Dark={StaticResource DarkSurfaceContainerLowest}}"
                HorizontalOptions="Center"
                Stroke="{AppThemeBinding Light={StaticResource LightOutlineVariant},
                                         Dark={StaticResource DarkOutlineVariant}}"
                StrokeThickness="1"
                VerticalOptions="Center"
                WidthRequest="{OnIdiom Phone=360,
                                       Desktop=450}">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="{StaticResource Large}" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="{StaticResource sContent}">
                    <Label
                        Style="{StaticResource MaterialTitleMedium}"
                        Text="Offline Access" />

                    <Grid
                        ColumnDefinitions="*,Auto"
                        ColumnSpacing="{StaticResource sContent}"
                        IsVisible="{Binding HasLocalUsers}">
                        <Picker
                            x:Name="LocalAccountPicker"
                            Title="Select Offline User"
                            IsVisible="{Binding HasLocalUsers}"
                            ItemsSource="{Binding LocalUsers}"
                            SelectedItem="{Binding SelectedUser}"
                            TitleColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                         Dark={StaticResource DarkOnSurfaceVariant}}">
                            <Picker.ItemDisplayBinding>
                                <Binding x:DataType="models:User" Path="Email.Value" />
                            </Picker.ItemDisplayBinding>
                        </Picker>
                        <controls:ImageButton
                            Grid.Column="1"
                            ButtonBgColor="{AppThemeBinding Light={StaticResource LightSecondaryContainer},
                                                            Dark={StaticResource DarkSecondaryContainer}}"
                            Command="{Binding RemoveOfflineUserCommand}"
                            IconColor="{AppThemeBinding Light={StaticResource LightOnSecondaryContainer},
                                                        Dark={StaticResource DarkOnSecondaryContainer}}"
                            IconGlyph="{x:Static m:MaterialOutlined.Delete}"
                            VerticalOptions="End" />
                    </Grid>

                    <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="{StaticResource sContent}" HorizontalOptions="Start">
                        <Label
                            Text="Offline Mode"
                            TextColor="{AppThemeBinding Light={StaticResource LightOnSurfaceVariant},
                                                        Dark={StaticResource DarkOnSurfaceVariant}}"
                            VerticalOptions="Center" />
                        <Switch
                            Grid.Column="1"
                            IsEnabled="{Binding IsConnectivityAvailable}"
                            IsToggled="{Binding IsOfflineModeEnabled}" />
                    </Grid>
                </VerticalStackLayout>
            </Border>
            </VerticalStackLayout>
        </Grid>
    </ScrollView>
</uranium:UraniumContentPage>

<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GoatVaultClient_v3.Controls.ImageButton"
             x:Name="this">
    <material:ButtonView WidthRequest="{StaticResource iconDesktopZone}" HeightRequest="{StaticResource iconDesktopZone}" PressedCommand="{Binding Command, Source={x:Reference this}}" BackgroundColor="Transparent">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">

                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="Transparent" />
                        </VisualState.Setters>
                    </VisualState>

                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={x:StaticResource LightSecondary}, Dark={x:StaticResource DarkSecondary}}"/>
                            <!-- <Setter Property="Color" Value="{AppThemeBinding Light={x:StaticResource LightOnSecondary}, Dark={x:StaticResource DarkOnSecondary}}" /> -->
                        </VisualState.Setters>
                    </VisualState>

                </VisualStateGroup>
            </VisualStateGroupList>
        </VisualStateManager.VisualStateGroups>
        <material:ButtonView.StrokeShape>
            <Ellipse />
        </material:ButtonView.StrokeShape>
        <Image VerticalOptions="Center" HorizontalOptions="Center" HeightRequest="{x:StaticResource iconDesktop}" WidthRequest="{x:StaticResource iconDesktop}">
            <Image.Source>
                <FontImageSource FontFamily="MaterialRounded" Glyph="{Binding IconGlyph, Source={x:Reference this}}" Color="{Binding Color}" Size="{x:StaticResource iconDesktopResolution}"/>
            </Image.Source>
        </Image>
    </material:ButtonView>
</ContentView>

<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GoatVaultClient_v3.ViewModels"
             xmlns:models="clr-namespace:GoatVaultClient_v3.Models"
             x:Class="GoatVaultClient_v3.EducationPage"
             x:DataType="vm:EducationPageViewModel"
             Title="EducationMenuPage">
    <CollectionView ItemsSource="{Binding Topics}"
                SelectionMode="Single"
                SelectedItem="{Binding CurrentTopic}"
                SelectionChangedCommand="{Binding OpenTopicCommand}"
                SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:EducationTopic">
                <Border Padding="20" Margin="10,5" StrokeShape="RoundRectangle 10" BackgroundColor="White">
                    <Label Text="{Binding Title}" TextColor="#512BD4" FontAttributes="Bold" />
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>